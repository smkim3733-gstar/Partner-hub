> 후속 상태: 아래의 배포 대기 설명은 선행 검증 당시의 기록입니다. 2026-08-31 버전 34의 공개 배포가 완료되었으며 주요 페이지 200·비로그인 상태 API 401을 확인했습니다. 새 파일의 계정 연결 보완은 [파일 계정 연결 안내](docs/FILE_ACCOUNT_LINKS_2026_08_31.md)를 참고하세요.

# 운영 반영 준비 — 2026-08-31

현재 로컬 작업본은 검증을 마쳤으며, 운영 배포 승인을 기다린다. GitHub와 Sites에 소스를 푸시하거나 새 버전을 저장·배포하지 않았다.

## 반영 내용

- 앞선 Windows 개발 환경 설정 및 공통 UI 정비.
- 파트너 자료의 조회·수정 권한을 서버에 저장된 담당 계정 ID로 우선 판단한다. 제출된 이름이나 계정 ID로 다른 계정의 기존 진행을 가져갈 수 없다.
- 신규 파트너 등록 자료에는 서버에서 본인의 계정 ID를 연결한다. 상담 FLOW가 확정한 담당 ID를 상태 저장 시에도 사용한다.
- ID가 없는 과거 자료는 이름이 정확히 한 계정에만 연결되는 경우에만 파트너 접근을 허용한다. 정지 계정도 동명이인 판별에 포함한다. 모호한 과거 자료는 삭제·자동 이관하지 않는다.
- 공유 일정은 담당자 외에는 시간 등 허용한 필드만 반환한다. 비공개 설명·회의 링크·담당 계정·진행 ID는 전달하지 않는다.
- 기존 파일 다운로드와 담당자 기준 삭제도 단순 동명이인 일치로 허용하지 않는다. 기존 업로더 본인의 삭제 권한과 대표 권한은 유지한다.

## 검증

- 전체 lint, 타입검사, 기존·신규 자동 테스트 87개, 빌드 통과.
- `tests/password-worker-smoke.mjs`: 실제 workerd와 격리 D1/R2에서 31개 확인.
- 본인 가입 → 승인 전 접근 거부 → 대표 API 승인·배정 → 비밀번호 로그인 → 본인 진행 조회 → 타인 진행·원본파일 접근 거부 → 정지 → 비밀번호 재설정·세션 폐기 확인.
- 오래된 대표 화면 저장이 새 등록을 덮어쓰지 않는지 확인. 현재 마이그레이션 4개를 두 번 적용해도 기존 상태가 보존되는지 확인.
- 격리 Worker의 외부 통신을 차단하고 요청 0건을 확인했다. 운영 쓰기·메일 발송·유료 AI 요청은 없다.
- 브라우저 클릭, 실제 파트너 계정, 공개 도메인의 로그인 쿠키 왕복, 실데이터 이관, 전체 보안감사는 이 검증에 포함하지 않는다.

## 운영 연결 확인

Sites에서 읽기 전용으로 확인한 기존 사이트는 **한기평 파트너 허브**이며 현재 공개 상태다. 주소는 `https://keve-partner-hub.smkim3733.chatgpt.site`이고, 조회 시 마지막 버전 번호는 33이다. 현재 연결 계정은 소유자다. `.openai/hosting.json`의 프로젝트 식별자와 `DB` / `AI_SOURCE_FILES` 연결을 보존한다.

공개는 사이트 접속 범위이며, 내부 고객 자료가 익명에게 공개된다는 의미는 아니다. 이 로컬 수정이 아직 운영 서버에 반영되지 않았다는 점도 구분한다. 운영 중인 코드에서 이번 현상이 실제 발생했거나 자료가 유출됐는지는 조사하지 않았다.

## 승인 후 진행 순서

1. 정확한 검증 소스 상태를 기록하고, 기존 Sites 소스 연결과 빌드 결과로 버전을 저장한다. 배포 전 공개 상태·최신 버전을 다시 확인한다. 다른 작업의 변경이 있으면 먼저 비교한다.
2. 기존 사이트의 공개 범위와 DB/R2 연결을 변경하지 않고 승인된 수정본을 배포한다. 이 변경은 새 DB 스키마를 추가하지 않으며, 현재 저장소의 추가형 마이그레이션 4개를 포함한다.
3. 배포 완료와 주요 경로 응답을 확인한다. 실패 시 반복해서 새 프로젝트를 만들거나 기존 데이터를 초기화하지 않는다.
4. 대표가 연락처를 확인한 파트너 1명을 선정한 뒤 실사용 검증을 별도로 진행한다. 실제 계정 등록, 승인, 링크 전달과 고객 자료 제출은 대상 및 작업 범위가 정해진 후에만 수행한다.

## 실사용 시 확인할 제한

동명이인이고 이름만 연결된 기존 업무·자료는 파트너 화면에서 보이지 않을 수 있다. 대표가 원본을 확인하고 계정별 연결을 정리해야 한다. 기존 파일 저장 테이블에는 담당 계정 ID 컬럼이 없으므로, 이 보호 조치만으로 모든 과거 파일의 ID 이관이 끝난 것은 아니다. 본인 업로드 자료도 이름 연결이 모호하면 다운로드가 제한될 수 있다.

기능 문제가 생겨 이전 버전을 다시 배포하면 해당 버전의 접근 제어 수준으로 돌아갈 수 있다. 자료를 지우거나 무조건 이전 버전으로 되돌리지 말고, 영향 계정과 변경 범위를 먼저 확인한다. 배포 후 AI 연결·정책 판단·메일 전송 등을 자동 완료로 표시하지 않는다.
