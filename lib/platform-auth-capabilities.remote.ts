// Public build capabilities only. No process environment or service credentials.
export const trustSitesIdentityHeaders = false;
export const sitesSignInEnabled = false;
// The server-only remote gate still rejects incomplete runtime configuration.
export const migrationPreviewOnly = false;
