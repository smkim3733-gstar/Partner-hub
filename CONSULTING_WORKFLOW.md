# 상담 FLOW 구현 및 게시 점검 — 2026-08-30

## 사용 흐름

`상담 FLOW · 보고서` 메뉴에서 기업을 선택한다. 기존 기업이 계정과 연결되지 않거나 동명이인이 있으면 대표가 `전체 진행현황 → 상담 FLOW 담당 계정`을 지정하고 DB 저장 완료 후 연다. 진행 시작 이후에는 해당 계정 ID로 접근권한을 고정한다. 자동으로 다른 계정에 재배정하지 않는다.

1. 1차 정밀진단보고서: 수동 본문·파일 등록 또는 승인된 Claude 생성. 담당 파트너가 동일 기업의 보고서 카드에서 즉시 조회한다.
2. 대표와 파트너: 각자 로그인해 최신 1차의 분석 완료를 기록한다. 보고서 개정 시 두 확인을 초기화한다.
3. 파트너가 동반 초회상담을 예약한다. 대표가 2차 보고서 및 실제 3차 PPTX/PDF를 등록한다.
4. 실제 초회상담 완료 후 Word(DOCX)·TXT 전사문을 첨부하거나 본문을 붙여넣는다. 기업·상담일·주요 금액, 활용 권한과 개인정보를 확인한 뒤 제출한다. 기업별 AI 허용 상태이면 수정·확인한 본문으로 4차 초안을 생성하고 내부 자료함에 저장한다. 원본 음성은 선택 첨부다.
5. 대표가 최신 4차를 검토하여 진행솔루션과 추가 필수 서류 필요 여부를 확정한다.
6. 서류요청 문안을 카톡·이메일로 직접 전달하고 발송 사실을 기록한다. 서류 첨부 수령 후 대표가 필수 서류를 검토한다.
7. 대표가 5차 견적서와 6차 경영자문용역계약서의 실제 본문·파일을 등록하고 출력한다. 단가·계약조건은 시스템이 임의로 만들지 않는다.
8. 계약상담은 동반·파트너 단독·대표 단독 중 선택한다. 참석자가 실제 서명본 및 약정 계약금을 등록한다.
9. 대표가 실제 입금을 확인한다. 부분입금은 잔액 대기이며, 약정 계약금 충족 후 수행 단계가 된다.
10. 수행 결과 확인, 관리 담당자와 다음 점검일을 기록하고 사후관리를 반복 업데이트한다.

추가상담과 계약 준비 중 서류요청은 반복 가능하다. 계약 후 계약 근거자료 변경은 잠근다. 새 범위의 계약·진단은 별도 진행으로 관리한다. 기존 타임라인/자료는 삭제하지 않으며 새 FLOW의 완료 사실로 자동 간주하지 않는다.

## 자동화 범위와 연결 한계

- 보고서 저장·담당 파트너 공유, 완료 조건에 따른 단계/다음 할 일/대표 상담일정 반영은 구현됨. 상대방 최신 변경은 새로고침으로 조회.
- 1차·4차 Claude API 호출 코드는 구현됨. 기업별 제3자 처리·마스킹·유료 이용 확인 후 허용해야 함. 기본은 중지.
- 음성 자동전사 서비스는 **미연결**. MP3/M4A/WAV만 등록하면 전사문 대기. 전사문을 입력하면 4차 생성 요청을 처리한다. 녹음 파일 자체는 Claude에 전송하지 않는다.
- AI는 PDF/PNG/JPEG/TXT/Markdown을 읽는다. 입력 파일 8개·합계 8MB 제한. 1차 DOCX는 본문 동시 입력 또는 PDF 변환 필요. PDF/이미지 개인정보는 자동 마스킹하지 않음.
- 2차·3차·5차·6차는 대표 준비 문서 등록 방식. PPT 자동 제작, 전자서명, 은행 입금 조회, 카카오/이메일 자동 발송은 구현 범위에 포함하지 않음.
- 대표 일정은 사이트 공유 일정에 반영. Google 자동 쓰기는 하지 않으며 기존 Google 캘린더 추가 링크는 유지.
- 실패/보류는 가짜 완료로 처리하지 않음. 자동 유료 재시도 없음. 기존 처리·과금 확인 후 대표가 재시도한다. 화면을 닫아 중단된 대기 작업은 상태 화면에서 계속할 수 있음.
- AI 결과는 담당 파트너와의 내부 검토 초안. 기업대표에게 고객용으로 전달하는 것은 대표 검토 후 별도 행위. 최신 법령/정책을 API가 외부 조회한다고 가정하지 않음.

## 상담 전사문 중심 입력

- `전사문·심화분석`에서 완료한 상담을 선택한다. DOCX/TXT는 5MB 이하, 분석 본문은 20~60,000자다. 초과한 내용을 임의로 자르지 않는다.
- 문서 선택 시 브라우저 안에서 본문을 읽고 수정 가능한 입력란에 표시한다. 선택만으로 서버 저장이나 외부 AI 전송을 하지 않는다. 저장 버튼을 누르면 원본 사본과 검토한 본문을 비공개로 보관한다.
- DOCX는 본문·표의 텍스트만 읽는다. 이미지·머리글·주석은 분석 본문에서 제외한다. 추적 변경, 숨김 텍스트, 외부 삽입, 매크로, 잘못된 XML, 과도한 압축 본문은 거부한다. 원본을 변경하거나 매크로를 실행하지 않는다.
- TXT는 UTF-8 및 BOM이 있는 UTF-16을 지원한다. 읽기 실패 시 기존 입력을 유지하고 변환·붙여넣기 안내를 표시한다. PDF/HWP/구형 DOC 첨부 본문 추출과 OCR은 이번 범위에서 지원하지 않는다.
- 새 파일·본문·상담 선택 변경 시 내용 확인을 다시 받는다. 확인 시각과 계정을 서버에 기록하되 이를 증빙 검증 완료로 표시하지 않는다. 본문 수정으로 첨부 원본이 바뀌지는 않으므로 첨부 사본의 개인정보도 별도 확인해야 한다.
- MP3/M4A/WAV는 25MB 이하 보조 첨부다. 음성만 보관하면 전사문 대기이며 음성 자동전사·AI 호출을 시작하지 않는다. 같은 기록에 나중에 DOCX/TXT 또는 붙여넣기로 전사문을 보완할 수 있다.
- 같은 상담의 동일 본문을 중복 등록하면 기존 생성 상태 확인을 안내한다. 같은 요청 재전송은 한 번만 저장한다. 생성 완료 후 수정은 새 버전으로 남기며 이전 첨부와 보고서를 보존한다.
- 스키마 변경 없이 기존 JSON 상태에 문서·음성 파일 ID와 본문 확인자 필드를 추가한다. 기존 `fileId`와 전사문은 그대로 읽을 수 있다. 두 파일 저장 중 실패하면 저장된 상태에서 참조되지 않는 새 파일만 정리한다.

## 저장·권한

- `consulting_flows` D1 테이블: 기업별 상태, 보고서 본문, 확인자, 상담, 서류 메타데이터, 생성 작업, 입금, 이력.
- 정수 revision으로 INSERT/UPDATE 경쟁을 감지한다. 같은 commandId 재전송은 한 번만 반영한다. 과거 서류 버전으로 작성한 견적/계약서가 새 서류 검토를 우회하지 못하도록 결정 ID와 서류 버전을 검증한다.
- R2의 `consulting-flow/` 비공개 원본은 인증된 파일 경로로만 내려받는다. 공개 URL/오브젝트 키는 응답에서 제외한다.
- 파트너 계정은 기존 인증과 활성 상태 검사를 유지. 기존 이름 기반 연결은 처음에만 사용하며 모호하면 거부한다. 이후 접근은 고정 member ID로 검사한다.
- 기존 `/api/state` 자동저장은 실제 FLOW 단계/입금 상태를 덮어쓸 수 없다. 대표 공유 일정에서 타 파트너는 회사·장소 없이 예약 시간만 본다.
- 대시보드 DB 조회는 SQL에서 보고서 본문/전사문을 제외해 모든 기업 원문을 메모리에 불러오지 않는다.
- 문서 삭제 API는 추가하지 않았다. AI 입력 제외는 원본을 보존하는 제외 처리다.

## 검증·게시 상태

실제 고객자료 및 유료 API 호출 없이 가상 fixture와 메모리 SQLite/R2 대체 저장소로 검증한다.

```sh
node node_modules/typescript/bin/tsc --noEmit
node --import ./tests/register.mjs --test tests/*.test.ts
pnpm build
```

테스트는 전체 업무 순서, 역할별 차단, 버전 무효화, 반복 상담/서류, 일부 입금, 저장 경쟁, 중복 요청, 비공개 파일 접근/출력, HTML escaping, 레거시 진행단계 우회 차단, AI 오류 상태를 포함한다. 전사문 추가 테스트는 가상 DOCX/TXT의 한글·금액·시간 보존, 오류·용량 제한, 서버 확인 필수, 문서·음성 동시 첨부, 전사문 보완, 중복 생성 방지와 가짜 API의 4차 보고서 저장을 검증한다. 실제 인증 프록시 로그인, 실고객의 유료 모델 호출, 실제 브라우저 인쇄·모바일 외관은 별도 확인 대상이다.

기존 공용 UI 컴포넌트의 전체 lint 오류는 이번 변경과 별개로 존재한다. 변경 파일 대상 lint와 타입/테스트/빌드를 따로 확인한다.

공개 사이트에는 **아직 게시하지 않음**. 대표의 이번 변경 게시 승인 후 기존 Sites 프로젝트에 배포하고 배포 상태를 확인해야 한다. 고객자료·비밀키는 소스 저장소에 포함하지 않는다.
