# 현재 구현과 다음 확인 순서

2026-09-01 기준. 이 문서는 현재 코드의 기능과 운영 경계를 정리한다. 날짜별 검증 문서는 당시 기록이며, 과거의 배포 대기 문구나 테스트 수를 현재 상태로 해석하지 않는다. 실제 반영 결과는 Sites 배포 이력과 로컬 `outputs/release/` 기록을 기준으로 확인한다.

## 현재 사용할 수 있는 기능

- 사이트 전용 이메일·비밀번호 가입, 대표 승인·직접 등록·정지, 일회용 비밀번호 설정. ChatGPT 로그인 경로도 유지한다.
- 담당 계정 ID 기준 진행·업무·원본 접근. 동명이인만으로 타인 자료 접근을 허용하지 않으며 모호한 레거시 자료는 자동 이관하지 않는다.
- 기업별 반복 협업신청, 신청 항목 저장, 서버 저장 확인 후 완료 안내, 같은 신청 재시도.
- 계정별 신청 텍스트 임시저장·복구. 첨부와 제출 동의는 자동 복구하지 않는다.
- 원본 업로드 응답 유실 시 재사용, 부분 실패 보존, 대표 전용 보관 목록·존재 검사.
- 오래 걸린 파일 요청의 계정·권한 재확인, 메타데이터 저장·삭제 결정 시 운영 상태 일치 확인. 중단된 업로드 원본은 보존하며, 다운로드 응답 직전 정지·삭제 상태를 다시 확인한다.
- 기존 신청·기업·담당 계정이 일치하는 원본만 대표 확인으로 회수. 원본 파일은 유지하고 자료 목록과 확인 이력을 함께 저장한다.
- 일반 저장에서 회수 연결·확인 사유·확인자·시각·이력의 위조·수정·누락·중복을 차단한다. 자료 카드에서 회수 사유와 한국시간 기준 시각을 읽을 수 있다. 검토 상태 변경은 허용한다.
- 상담 FLOW, 반복 상담·서류 요청, 보고서 등록, 대표/파트너 확인, 견적·계약·서명·입금의 개별 확인.
- 상담 FLOW 저장 직전 권한·명단 재확인, 계정·명령 내용·첨부 해시가 일치하는 동일 요청 재시도. 외부 AI 요청 직전에도 호출 계정과 작업 승인 상태를 확인한다.
- 승인된 외부 처리·비용 확인을 거친 AI 1차/4차 초안 요청 경로. 보고서 최종 승인과 고객 전달은 대표 판단이다.
- 새 저장소의 대표 선택형 기준선 초기화, 가상 예시를 제외한 대표 운영 지표, 관리자 전용 저장 용량·잠정 경보 표시.
- 저장 경로 3곳의 개인정보 없는 일별 충돌 집계와 대표 전용 최근 7일 지표. 기존 409·입력 보존·수동 재시도 방식은 유지한다.
- 저장 충돌 뒤 24시간 동안 열린 화면 메모리에서만 전달하는 익명 단일사용 회수 영수증. DB에는 토큰 해시와 서버 경로·역할·충돌 종류·시각만 보관하고, 대표에게 경로별 관측 회수율과 최소 5건 이상인 거친 시간 구간만 표시한다.

## 검증과 배포 경계

현재 소스는 자동 테스트 217개와 격리 workerd/D1/R2 검증 114개를 기준으로 확인한다. 최신 파일럿 준비 고도화의 lint·타입검사·빌드 결과와 운영 경계는 [파트너 파일럿 준비 고도화](PILOT_READINESS_ENHANCEMENT_2026_09_01.md) 및 [저장 충돌 운영 지표](SAVE_CONFLICT_OBSERVABILITY_2026_09_01.md)에 구분해 남긴다. 기존 DB/R2 연결과 추가형 DB 마이그레이션을 유지한다.

운영 고객 파일·명단을 개발용으로 읽거나 복사하지 않는다. 가상 데이터로 테스트하며 실제 계정 등록·메일 발송·유료 AI를 검증 수단으로 사용하지 않는다. 서비스 전체 침투시험, 실제 파트너의 로그인 쿠키 왕복과 전체 레거시 이관은 완료로 표시하지 않는다.

별도 승인에 따른 [로컬 가상 데이터 화면 점검](LOCAL_SCREEN_REVIEW_2026_09_01.md)으로 로그인·신청·첨부·회수·FLOW 초기 역할과 재시도를 확인했다. 파트너 홈의 고정 예시 목록·수치를 허용된 저장 데이터로 연결하고, FLOW의 비정상 응답을 한국어 재시도 안내로 보완했다. 키보드 전용 조작 및 전체 FLOW 단계의 브라우저 검증은 미완료로 구분한다. 기존 3항목 자동 점검은 재개하지 않는다. 후속 화면 수정의 반영 상태는 `outputs/release/ui-screen-deployment.json`을 기준으로 한다.

[키보드·대화상자 후속 점검](KEYBOARD_DIALOG_REVIEW_2026_09_01.md)에서는 자료 등록창·업무 추가창의 초점 이동, Escape 닫기·초점 복원, 업로드 중 닫기 차단과 짧은 모바일 화면 잘림을 보완했다. 표준 Tab 이동·Enter 실행의 도구 한계는 별도로 기록했고 전체 키보드 검증 완료로 간주하지 않는다. 이 수정의 운영 반영 결과는 `outputs/release/keyboard-dialog-deployment.json`을 기준으로 한다.

## 이어서 진행할 점검

사용자의 별도 승인과 Duet 검수에 따라 저장 충돌의 24시간 익명 회수 영수증을 구현했다. 영수증 DB 기록이 성공한 경우에만 409 응답에 평문 토큰을 전달하고, 같은 경로·역할의 다음 성공 요청에서 한 번만 회수한다. 상태 저장과 대표 직접등록 화면은 토큰을 열린 화면 메모리에만 유지하며, 공개 자가가입은 API 계약만 제공한다. 만료·사용된 익명 영수증 행만 자동 정리하고 고객 자료와 집계 보존 정책은 바꾸지 않는다. 경로별 발급·관측 회수율을 대표에게만 제공하며 정확한 일별 시간과 5건 미만 시간 구간은 숨긴다. Node 검사 217개, 격리 workerd/D1/R2 검사 114개, 타입·lint·빌드 통과 후 커밋 `9b0d4c9`를 GitHub `main`과 기존 공개 Sites 버전 64에 반영했다. 상세 경계와 배포 결과는 [저장 충돌 운영 지표](SAVE_CONFLICT_OBSERVABILITY_2026_09_01.md) 및 `outputs/release/conflict-recovery-receipt-deployment.json`에 기록한다.

별도 사용자 승인과 Duet 검수에 따라 [저장 충돌 운영 지표](SAVE_CONFLICT_OBSERVABILITY_2026_09_01.md)를 구현했다. 운영 상태 저장·공개 자가가입·대표 파트너 등록의 충돌을 식별정보 없이 한국시간 일별로 집계하고 대표에게만 최근 7일 건수를 표시한다. 용량 오류는 충돌 건수에서 제외하며 지표 기록 실패가 기존 409 응답을 바꾸지 않는다. Node 검사 211개, 격리 workerd/D1/R2 검사 110개, 타입·lint·빌드가 통과했다. Duet 차단·중대 의견을 모두 반영했으며 네이티브 Windows의 최종 차이 검토 제한을 기록했다. 사용자 승인 후 GitHub `main`과 기존 공개 Sites 버전 63에 반영했고 결과는 `outputs/release/save-conflict-observability-deployment.json`에 기록한다.

별도 사용자 요청과 Duet 검수에 따라 [파트너 파일럿 준비 고도화](PILOT_READINESS_ENHANCEMENT_2026_09_01.md)를 수행했다. 새 저장소의 가상 데이터 자동 저장을 막고 대표가 빈 운영·가상 예시 기준선을 선택하도록 했으며, 고정 시드 ID만 가상 예시로 분류해 대표 지표에서 파생 제외한다. 관리자에게만 실제 저장 원문과 다음 요청의 용량·잠정 경보를 표시하며 파트너가 예약 시드 ID로 운영 기록을 만들지 못하게 했다. Node 검사 209개와 격리 workerd/D1/R2 검사 110개, 타입·lint·빌드가 통과했다. Duet 차단·중대 의견은 모두 해결했으며 네이티브 Windows의 격리 검사 제한으로 최종 차이 검토 완료 판정은 남겨 두었다. 사용자 승인 후 기존 공개 범위와 DB/R2 연결을 유지한 Sites 버전 62 운영 배포를 완료했으며 결과는 `outputs/release/pilot-readiness-deployment.json`에 기록한다.

별도 후속 요청으로 [Step 0 응답 저장 직전 상태 재검증](STEP_ZERO_FINAL_STATE_REVIEW_2026_09_01.md)을 수행했다. 외부 응답 대기 중 동의가 철회되거나 필수 R2 원본이 사라지면 완료 결과를 저장하지 않고 요청을 실패 상태로 격리한다. 같은 요청 ID의 자동 재호출도 차단한다. Node 검사 202개와 격리 workerd/D1/R2 검사 110개 통과 및 반영 결과는 해당 문서와 `outputs/release/step-zero-final-state-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [Step 0 중복 실행·응답 유실 점검](STEP_ZERO_IDEMPOTENCY_REVIEW_2026_09_01.md)을 수행했다. 생성 요청 ID·입력 지문·진행별 실행 잠금을 서버에 적용해 동시 요청은 외부 호출 전에 차단하고, 완료 응답 유실 재시도는 저장 결과를 재사용한다. 불확실한 실패 요청은 같은 ID로 자동 재호출하지 않는다. Node 검사 202개와 격리 workerd/D1/R2 검사 110개 통과 및 반영 결과는 해당 문서와 `outputs/release/step-zero-idempotency-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [Step 0 실행 직전 원본·동의 재검증](STEP_ZERO_PREFLIGHT_REVIEW_2026_09_01.md)을 수행했다. 서버가 외부 AI 요청 직전에 현재 진행·담당 계정·필수 자료·모든 동의와 D1/R2 원본 존재를 다시 확인하도록 보완했다. 메타데이터만 남은 카드, 다른 반복 진행 자료, 사라진 R2 원본, 철회된 녹취 동의는 외부 호출 전에 차단한다. Node 검사 202개와 격리 workerd/D1/R2 검사 110개 통과 및 반영 결과는 해당 문서와 `outputs/release/step-zero-preflight-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [자료 완료상태·AI 판정 근거 점검](DOCUMENT_EVIDENCE_REVIEW_2026_09_01.md)을 수행했다. 보안 저장 파일이 없는 요청 카드의 제출·검토 완료 변경을 막고, AI 사전점검도 실제 저장 파일이 있는 완료 자료만 근거로 사용한다. Node 검사 201개 통과와 반영 결과는 해당 문서 및 `outputs/release/document-evidence-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [AI 진단 검토업무 진행 연결 점검](DIAGNOSIS_TASK_LINK_REVIEW_2026_09_01.md)을 수행했다. 동일 기업의 반복 신청에서 대표 검토업무가 합쳐지지 않도록 진행 ID 기준 중복 확인과 업무 연결을 적용했다. Node 검사 198개 통과와 반영 결과는 해당 문서 및 `outputs/release/diagnosis-task-link-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [AI 진단 사전점검 자료 연결 점검](DIAGNOSIS_PREFLIGHT_REVIEW_2026_09_01.md)을 수행했다. 동일 기업의 반복 진행·다른 담당 자료가 판정 근거에 섞이지 않도록 진행 ID·담당 계정 연결을 적용하고, 초안 검토대기 등록 직전에 최신 자료로 A 판정을 다시 확인한다. Node 검사 197개 통과와 반영 결과는 해당 문서 및 `outputs/release/diagnosis-preflight-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [견적·계약 진입 경로 점검](QUOTE_CONTRACT_ROUTE_REVIEW_2026_09_01.md)을 수행했다. 입력을 저장하지 않으면서 호출되지도 않던 옛 초안 모달을 제거하고, 권한이 있는 사용자의 견적·계약 상태 확인을 실제 단계·권한 검사가 적용되는 상담 FLOW로 일원화했다. 권한 없는 파트너의 탭·버튼 비노출도 가상 계정으로 확인했다. Node 검사 195개 통과와 반영 결과는 해당 문서 및 `outputs/release/quote-contract-route-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [기존 서류요청 등록 화면 점검](LEGACY_DOCUMENT_REQUEST_REVIEW_2026_09_01.md)을 수행했다. 저장되지 않는 선택·안내 입력과 고정 요청번호를 정리하고, 날짜·서류명 정규화와 진행별 요청중·보완필요 서류의 중복 생성을 차단했다. Node 검사 195개 통과와 반영 결과는 해당 문서 및 `outputs/release/legacy-document-request-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [기존 상담 등록 화면 점검](LEGACY_CONSULTATION_REVIEW_2026_09_01.md)을 수행했다. 취소 상태의 후속 업무 생성, 저장되지 않는 입력란, 고정 날짜·기한과 실제 구현보다 넓은 캘린더 안내를 수정했다. 기본정보와 한국시간을 상담 기록에 보존하고, 일정 확정과 상담 완료의 일정·후속 업무 생성을 분리했다. Node 검사 192개 통과와 반영 결과는 해당 문서 및 `outputs/release/legacy-consultation-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [일정 날짜 표시 점검](SCHEDULE_DISPLAY_REVIEW_2026_09_01.md)을 수행했다. 고정 주간 범위 밖의 일정 누락을 날짜별 목록으로 수정하고 연도 미상·잘못된 날짜의 외부 등록을 제한했다. 실제 구현과 다른 연결·동기화·예약 가능 안내를 바로잡았으며 가상 일정의 공개범위도 확인했다. Node 검사 187개 통과와 반영 결과는 해당 문서 및 `outputs/release/schedule-display-deployment.json`을 기준으로 한다.

별도 후속 요청으로 [대표 대시보드 점검](ADMIN_DASHBOARD_REVIEW_2026_09_01.md)을 수행했다. 고정 예시 통계를 조회된 진행·자료함 집계로 바꾸고, 긴급·장기 미진행 목록에서 선택한 진행으로 이동하도록 연결했다. 빈 상태와 동일 기업의 반복 진행을 가상 데이터로 확인했다. 이 변경의 반영 결과는 `outputs/release/admin-dashboard-deployment.json`에 기록한다.

별도 후속 요청에 따른 [상담 FLOW 후반 화면 점검](LATE_FLOW_SCREEN_REVIEW_2026_09_01.md)으로 솔루션 확정·필수서류 보완·5/6차 문서·가상 계약·분할 입금 재시도·사후관리의 대표 경로를 확인했다. 조회 실패의 영문 오류와 정상 재조회 뒤 남는 오류 안내를 수정했다. 모든 분기·실제 거래·전체 키보드 검증과는 구분하며, 반영 결과는 `outputs/release/flow-late-deployment.json`에 기록한다.

이 목록은 기존 기능을 보호하는 점검이며 신규 외부 서비스 도입이나 실제 고객 업무를 승인하는 목록이 아니다. 한 항목씩 재현 가능한 코드·격리 검증을 수행하고, 필요한 수정이 있으면 검증 후 기존 사이트에 반영한다. 변경이 필요 없으면 그 근거를 기록한다.

1. [x] 원본 회수 완료·응답 유실·재시도 시 화면 저장 잠금과 최신 상태 반영 경로 점검. 응답 유실 뒤 잠금 유지·동일 요청 재시도와 명시적 최신 화면 확인을 보완했다. [검증 기록](RECOVERY_LOCK_REVIEW_2026_08_31.md)
2. [x] 원본 업로드·다운로드·삭제의 동시 요청 및 계정 정지 시점에 대한 권한·보존 검증. 늦은 권한 변경·삭제 상태 재확인, 조건부 저장·삭제 결정, 처리 중 업로드의 삭제 충돌, 레거시 삭제 실패 후 접근 차단을 보완했다. [검증 기록](FILE_OPERATION_REVIEW_2026_09_01.md)
3. [x] 상담 FLOW의 단계별 권한과 중복 요청 보호를 기존 테스트와 대조해 누락된 회귀 검증 보완. 처리 중 정지·권한 변경, 요청 번호의 다른 계정·내용·첨부 재사용, AI 전송 직전 권한 재확인을 보완했다. 상담 취소·발송 확인 기록의 누락된 검증도 추가했다. [검증 기록](FLOW_PERMISSION_REVIEW_2026_09_01.md)

승인된 순차 점검 3항목의 코드 점검을 모두 마쳤다. 최종 운영 반영·자동 후속 실행 일시 중지 결과는 Sites 배포 이력과 `outputs/release/flow-permissions-deployment.json`에 기록한다. 별도 사용자 요청 전에는 이 목록에 새 항목을 추가하거나 자동 점검을 재개하지 않는다.

위 점검이 완료되면 임의의 기능이나 반복 업무를 추가하지 않는다. 사용자 결정이 필요한 다음 사항은 기존 설정을 유지하고, 실제로 작업의 진행을 막을 때만 구체적인 대상과 영향 범위를 제시한다.

## 사용자 결정 또는 별도 범위가 필요한 일

- 실제 파트너 한 명을 선정한 가입·승인·로그인 실사용 검증과 연락처 확인.
- 진행번호·담당 ID가 없는 과거 원본의 수동 배정, 보관 기간·삭제 정책 결정, 실제 데이터 이관·삭제.
- 이메일·카카오톡 발송, 일정 양방향 동기화, 음성 자동전사, 전자서명·입금 조회 연결.
- 유료 AI·외부 처리의 키·잔액·모델·기업별 자료 전송 및 비용 승인. 메일 소유권 인증·MFA 도입 정책.

코드 개선 완료와 실제 운영 절차 완료를 구분한다. 중요한 정책 결정을 임의로 정하거나 고객에게 연락하지 않는다.
